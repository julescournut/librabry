{% extends 'back/index.html.twig' %}

{% block backContent %}
<div class="row">
<div class="comment-form-wrap my-form">
    <div class="col-md-12 heading-section text-center ftco-animate">
      <h2>Modifier genre {{ genre.titre }}</h2>
    </div>
  <form method="POST" class="p-4 bg-light">
      <div class="form-group">
        <label for="titre">Titre *</label>
        <input required type="text" class="form-control" value="{{ genre.titre }}" name="titre" id="titre">
      </div>

    <div class="form-group">
      <input type="submit" value="Valider" class="btn py-3 px-4 btn-primary">
    </div>

  </form>
</div>
</div>
{% endblock %}
